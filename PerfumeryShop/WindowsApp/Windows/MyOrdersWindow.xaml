<Window x:Class="PerfumeryShop.WindowsApp.Windows.MyOrdersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PerfumeryShop.WindowsApp.Windows"
        mc:Ignorable="d"
       Title="Мои заявки"
        Height="600"
        Width="950"
        WindowStartupLocation="CenterScreen"
        Background="#FFF6F1">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,15">
            <DockPanel>
                <TextBlock Text="Мои заявки"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="#7A3E48"
                           DockPanel.Dock="Left"/>

                <TextBlock x:Name="tbCount"
                           FontSize="16"
                           Foreground="Gray"
                           HorizontalAlignment="Right"/>
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ListBox x:Name="lbOrders"
                     BorderThickness="0"
                     Background="Transparent"
                     HorizontalContentAlignment="Stretch">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>

                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Width" Value="420"/>
                        <Setter Property="Height" Value="220"/>
                        <Setter Property="Margin" Value="8"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListBox.ItemContainerStyle>

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White"
                                BorderBrush="#E7D8D8"
                                BorderThickness="1"
                                CornerRadius="16"
                                Padding="12">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Width="90"
                                            Height="120"
                                            CornerRadius="12"
                                            Background="#FFF1F1"
                                            ClipToBounds="True"
                                            VerticalAlignment="Center">
                                        <Image Source="{Binding ImagePath}" Stretch="UniformToFill"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding ProductName}"
                                                   FontSize="18"
                                                   FontWeight="Bold"
                                                   Foreground="#2D1F23"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,8"/>

                                        <TextBlock Text="{Binding OrderDateText}"
                                                   FontSize="13"
                                                   Foreground="#5C4B4F"
                                                   Margin="0,0,0,6"/>

                                        <Border Background="#FCE8E8" CornerRadius="8" Padding="8,4" Margin="0,0,0,6">
                                            <TextBlock Text="{Binding StatusText}"
                                                       FontWeight="SemiBold"
                                                       Foreground="#8B3A3A"/>
                                        </Border>

                                        <TextBlock Text="{Binding CommentText}"
                                                   Foreground="#7D6A6E"
                                                   FontSize="12"
                                                   TextWrapping="Wrap"
                                                   MaxHeight="48"/>
                                    </StackPanel>
                                </Grid>

                                <Button Grid.Row="1"
                                        Content="Удалить заявку"
                                        Height="34"
                                        Margin="0,12,0,0"
                                        Tag="{Binding Id}"
                                        Click="DeleteOrder_Click"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </ScrollViewer>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="12" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Назад" Width="120" Height="36" Margin="5" Click="Back_Click"/>
                <Button Content="Обновить" Width="120" Height="36" Margin="5" Click="Refresh_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
